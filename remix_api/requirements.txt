services:
  - type: web
    name: remix-api
    env: python
    buildCommand: |
      apt-get update && apt-get install -y ffmpeg
      pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: GOOGLE_APPLICATION_CREDENTIALS
        value: firebase_credentials.json
    plan: free
    autoDeploy: true

  - type: worker
    name: remix-worker
    env: python
    buildCommand: |
      apt-get update && apt-get install -y ffmpeg
      pip install -r requirements.txt
    startCommand: python remix_worker.py
    envVars:
      - key: GOOGLE_APPLICATION_CREDENTIALS
        value: firebase_credentials.json
    plan: free
    autoDeploy: true
